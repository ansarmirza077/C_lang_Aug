
#include <stdio.h>
    void main ()
    {
 
        int number[30],i, j;                           //i and j are loop counter
 
        int  even=0,odd=0, size;                      // variable even and odd are to store even and odd count respectively
        printf("Enter the number of size of an array\n");
        scanf("%d", &size);
 
        printf("Enter the numbers \n");
        for (i = 0; i < size; ++i)                             //fetching elements from user
	        scanf("%d", &number[i]);
 
        for (i = 0; i < size; ++i)  
        {
           if(number[i]%2==0) even++;                              //if reminder after division by 2 yeilds 0 then increment even ;
           else odd++;
        }
      
      printf("there are %d even elements and\n%d odd elements in this array",even,odd);
      }
      
      OUTPUT:

Enter the number of size of an array 5
Enter the numbers 
1 2 3 4 5
there are 2 even elements and
3 odd elements in this array 
