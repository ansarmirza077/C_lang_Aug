#include<stdio.h>
#include<string.h>

int main()
{
    int i;
    char str[] = "Program to copy string";
    char b[50];
   /* for(i=0;i<sizeof(a[50]);i++)
    {
        b[i]=a[i];
    }
    for(i=0;i<sizeof(a[50]);i++)
    printf("%s",b[i]);*/
    char length;
    //length = sizeof str;
printf("size of a is :%d",sizeof str);

}